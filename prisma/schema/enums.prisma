// ! Enumerations for

// * User model
enum ActiveStatus {
  ACTIVE
  BLOCKED
}

enum Role {
  USER
  AUTHOR
  ADMIN
}

// * Post model
enum PostStatus {
  DRAFT
  PUBLISHED
  ARCHIVED
}

// * Comment model
enum CommentStatus {
  APPROVED
  REJECTED
}
